name: build-site

on:
  push:
    branches: [ source ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: cnerg/cnerg.github.io-deps

    steps:
      - uses: actions/checkout@v2

      - name: view the Makefile
        run: cat Makefile
      - name: build the site
        run: make docker-gh-stage
#          make docker-gh-stage,
#          make docker-gh-push